export const IOSRedirectHandler = () => {
  // Componente simplificado - não há mais necessidade de redirecionamento complexo
  // Ambas as rotas /dashboard e /painel agora usam o mesmo DashboardLite
  return null;
};